# Next.js and Drizzle both resolve peer deps that pnpm hoists strictly by default.
# Keeping this narrow rather than disabling strictness wholesale.
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
