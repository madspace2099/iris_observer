/**
 * `@observer/ui` — the Observer design system.
 *
 * Tokens in CSS, components in React, no utility framework and no component
 * library: the brief rules out a stock appearance, and a system whose styles
 * live in class strings scattered through markup cannot be reviewed as a
 * system (see `components.css`).
 *
 * Import the two stylesheets once, in the application's root layout:
 *
 *   import "@observer/ui/tokens.css";
 *   import "@observer/ui/components.css";
 */

export * from "./primitives";
export * from "./intelligence";
